node_modules
dist
.env
.env.local
# Runtime uploads directory (at the api package root only) — the bare
# `storage/` form also matched `src/storage/` and hid our source code.
/storage/
!/storage/.gitkeep
