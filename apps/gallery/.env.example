# Leave blank in dev — Vite proxies /api to the backend (see vite.config.ts).
# Set to the API origin in production, e.g. https://api.darkdrive.zenux.live
VITE_API_URL=

# `vite preview` port. Override on the host if needed.
PORT=4300
